# slugify

```javascript
var slugify = require('@ryanburnette/slugify');

var title = "60% of the time, it works every time";

var slug = slugify(title);

console.log(slug);
// => "60-of-the-time-it-work-every-time"
```
